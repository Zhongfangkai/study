iScroll-lite.js  导航条横向滑动居中的插件

插件实例代码
new IScroll(id, {
        scrollX: true,
        tap: true,
        /*初始化下划线位置*/
        init_line: function (self) {},
        /*点击事件的回调函数*/
        callBack: function (self, target) {}
});

api-common.js   h5 混合APP开发公共方法的封装插件